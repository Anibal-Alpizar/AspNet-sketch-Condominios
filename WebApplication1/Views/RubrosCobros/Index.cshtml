@model IEnumerable<WebApplication1.Models.PLAN_COBRO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_PLAN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RUBRO_COBRO.NOMBRE_RUBRO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RUBRO_COBRO.NOMBRE_RUBRO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PLAN_COBRO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PLAN_COBRO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PLAN_COBRO })
        </td>
    </tr>
}

</table>
